global:
  scrape_interval: 5s
  scrape_timeout: 5s

scrape_configs:
  - job_name: "fastapi-image-description"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "localhost:8000"